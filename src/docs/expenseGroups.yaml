paths:
  /expense-groups:
    get:
      tags:
        - expense-groups
      summary: Gets all expense groups
    post:
      tags:
        - expense-groups
      summary: Creates a new expense group associated with user ID
  /expense-groups/{expenseGroupId}:
    get:
      tags:
        - expense-groups
      summary: Gets an expense group associated with user ID
    patch:
      tags:
        - expense-groups
      summary: Updates an expense group associated with user ID
    delete:
      tags:
        - expense-groups
      summary: Deletes an expense group associated with user ID
  /expense-groups/{expenseGroupId}/expenses:
    get:
      tags:
        - expense-groups
      summary: Gets all expenses associated with an expense group ID
    post:
      tags:
        - expense-groups
      summary: Creates a new expense associated with an expense group
  /expense-groups/{expenseGroupId}/expenses/{expenseId}:
    patch:
      tags:
        - expense-groups
      summary: Updates an expense associated with an expense group
    delete:
      tags:
        - expense-groups
      summary: Deletes an expense associated with an expense group
